# Python CI Template - Повторно используемый шаблон CI/CD

![CI Status](https://img.shields.io/badge/CI-reusable-blue)
![GitHub Actions](https://img.shields.io/badge/GitHub%20Actions-compatible-green)
![Python](https://img.shields.io/badge/python-3.9%2B-blue)

## Описание

Универсальный шаблон CI/CD конвейера для Python-проектов, который можно повторно использовать в любых репозиториях. Следует принципам **CMMI** и стандарту **ISO/IEC 29110** (повторяемость и стандартизация процессов).

## Цель проекта

Создать шаблон CI-конвейера, который можно использовать повторно в любых Python-проектах, следуя принципам CMMI и ISO/IEC 29110 (повторяемость и стандартизация процессов).

## Возможности шаблона

### 🔍 Проверка качества кода (Linting)
- **flake8** — проверка стиля кода по PEP 8
- **pylint** — статический анализ кода
- **black** — проверка форматирования кода
- **isort** — проверка сортировки импортов

### ✅ Автоматическое тестирование
- Поддержка нескольких версий Python (3.9, 3.10, 3.11+)
- Запуск тестов с помощью **pytest**
- Параллельное выполнение тестов
- Кэширование зависимостей для ускорения сборки

### 📊 Покрытие кода
- Генерация отчетов о покрытии кода
- Поддержка форматов: terminal, XML, HTML
- Автоматическая загрузка отчетов как артефактов

### 🔒 Проверка безопасности
- **safety** — проверка уязвимостей в зависимостях
- **bandit** — сканирование кода на проблемы безопасности

### 📈 Сводный отчет
- Агрегация результатов всех этапов
- Статус выполнения каждого шага

## Структура проекта

```
python-ci-template/
├── .github/
│   └── workflows/
│       ├── ci-template.yml      # Основной повторно используемый шаблон
│       └── example-usage.yml    # Примеры использования
├── docs/
│   ├── USAGE.md                 # Подробная инструкция
│   └── COMPARISON.md            # Сравнение до/после стандартизации
└── README.md
```

## Быстрый старт

### Шаг 1: Добавьте шаблон в ваш репозиторий

Создайте файл `.github/workflows/ci.yml` в вашем проекте:

```yaml
name: CI Pipeline

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  ci:
    uses: <username>/python-ci-template/.github/workflows/ci-template.yml@main
```

### Шаг 2: Настройте параметры (опционально)

```yaml
jobs:
  ci:
    uses: <username>/python-ci-template/.github/workflows/ci-template.yml@main
    with:
      python-versions: '["3.10", "3.11"]'
      run-linting: true
      run-tests: true
      run-coverage: true
      requirements-file: 'requirements.txt'
      source-directory: 'src'
      test-directory: 'tests'
```

## Параметры конфигурации

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `python-versions` | string (JSON array) | `["3.9", "3.10", "3.11"]` | Версии Python для тестирования |
| `run-linting` | boolean | `true` | Включить проверку стиля кода |
| `run-tests` | boolean | `true` | Включить запуск тестов |
| `run-coverage` | boolean | `true` | Включить отчет о покрытии |
| `requirements-file` | string | `requirements.txt` | Путь к файлу зависимостей |
| `source-directory` | string | `src` | Директория с исходным кодом |
| `test-directory` | string | `tests` | Директория с тестами |

## Примеры использования

### Базовое использование

```yaml
jobs:
  ci:
    uses: <username>/python-ci-template/.github/workflows/ci-template.yml@main
```

### Только тестирование (без линтинга)

```yaml
jobs:
  ci:
    uses: <username>/python-ci-template/.github/workflows/ci-template.yml@main
    with:
      run-linting: false
      python-versions: '["3.11"]'
```

### Полная конфигурация

```yaml
jobs:
  ci:
    uses: <username>/python-ci-template/.github/workflows/ci-template.yml@main
    with:
      python-versions: '["3.9", "3.10", "3.11", "3.12"]'
      run-linting: true
      run-tests: true
      run-coverage: true
      requirements-file: 'requirements/dev.txt'
      source-directory: 'app'
      test-directory: 'tests'
```

## Связь со стандартами DevOps

| Стандарт | Применение в шаблоне | Преимущество |
|----------|---------------------|--------------|
| **ISO/IEC 29110** | Повторяемость процессов | Единый стандарт CI для всех проектов |
| **CMMI Level 3** | Стандартизация и оптимизация | Определенный и управляемый процесс |
| **ISO/IEC 12207** | Жизненный цикл ПО | Автоматизация этапов разработки |
| **ISO/IEC 25010** | Качество ПО | Контроль качества через тесты и линтинг |
| **IEEE 829** | Документирование тестирования | Отчеты о покрытии и результатах |

## Сравнение: До и После стандартизации

### До стандартизации CI

❌ **Проблемы:**
- Каждый проект имеет свою конфигурацию CI
- Дублирование кода в workflow файлах
- Сложность поддержки и обновления
- Несогласованность проверок качества
- Отсутствие единых стандартов

### После стандартизации CI

✅ **Преимущества:**
- Единый шаблон для всех Python-проектов
- Централизованное управление CI процессом
- Легкое обновление всех проектов одновременно
- Гарантированное качество кода
- Соответствие стандартам DevOps

## Преимущества повторно используемых компонентов

### 🚀 Ускорение DevOps-цикла

- **Быстрый старт новых проектов** — не нужно настраивать CI с нуля
- **Сокращение времени на поддержку** — изменения вносятся в одном месте
- **Стандартизация процессов** — все проекты следуют одним правилам

### 📈 Повышение качества

- **Единые стандарты качества** — все проекты проверяются одинаково
- **Автоматическое обновление** — улучшения применяются ко всем проектам
- **Снижение ошибок** — проверенный шаблон минимизирует риски

### 💰 Экономия ресурсов

- **Меньше дублирования кода** — один шаблон вместо множества копий
- **Упрощение обучения** — новые разработчики изучают один стандарт
- **Снижение затрат на поддержку** — централизованное управление

## Интеграция с существующими проектами

### Для проекта python-ci-lab

```yaml
# .github/workflows/ci.yml
name: CI

on: [push, pull_request]

jobs:
  ci:
    uses: <username>/python-ci-template/.github/workflows/ci-template.yml@main
    with:
      source-directory: 'src'
      test-directory: 'tests'
```

### Для проекта flask-docker-app

```yaml
# .github/workflows/ci.yml
name: CI

on: [push, pull_request]

jobs:
  ci:
    uses: <username>/python-ci-template/.github/workflows/ci-template.yml@main
    with:
      source-directory: '.'
      test-directory: 'tests'
      python-versions: '["3.11"]'
```

## Расширение шаблона

Шаблон можно легко расширить дополнительными проверками:

- Проверка типов (mypy)
- Проверка документации (pydocstyle)
- Анализ сложности кода (radon)
- Проверка лицензий зависимостей
- Автоматическое создание релизов

## Требования

- GitHub Actions (или совместимая CI/CD платформа)
- Python 3.9 или выше
- pytest для тестирования
- Структурированный проект с директориями `src/` и `tests/`

## Лучшие практики

1. **Версионирование**: Используйте теги для версий шаблона (`@v1`, `@v2`)
2. **Тестирование**: Тестируйте изменения шаблона перед применением
3. **Документирование**: Документируйте все изменения в CHANGELOG
4. **Обратная совместимость**: Сохраняйте совместимость при обновлениях
5. **Мониторинг**: Отслеживайте использование шаблона в проектах

## Вклад в развитие

Приветствуются предложения по улучшению шаблона:

1. Создайте issue с описанием предложения
2. Сделайте fork репозитория
3. Внесите изменения в отдельной ветке
4. Создайте pull request с описанием изменений

## Автор

Практическая работа по курсу DevOps and Continuous Integration Standards

## Лицензия

MIT License

---

## Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Reusable Workflows Guide](https://docs.github.com/en/actions/using-workflows/reusing-workflows)
- [Python Testing Best Practices](https://docs.pytest.org/en/stable/)
- [ISO/IEC 29110 Standard](https://www.iso.org/standard/51153.html)
